<?xml version="1.0" encoding="utf-8"?>

<mvvm:BaseView xmlns="http://xamarin.com/schemas/2014/forms"
               xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
               xmlns:mvvm="clr-namespace:XLabs.Forms.Mvvm;assembly=XLabs.Forms"
               x:Class="HelloWorld.Pages.GeolocatorPage">
    <StackLayout Orientation="Vertical">
      <!--<ActivityIndicator IsEnabled="{Binding LoadingPosition}" IsVisible="{Binding LoadingPosition}"></ActivityIndicator>-->
       <Label Text="{Binding DisplayMainModel.Title}" />
      <!-- <Button Text="Get Position" Command="{Binding MyCommand}" />-->
        <Entry Text="{Binding DisplayMainModel.Latitude}"/>
        <Entry Text="{Binding DisplayMainModel.Longitude}"/>
        <Entry Text="{Binding DisplayMainModel.Timestamp}"/>
    </StackLayout>
</mvvm:BaseView>